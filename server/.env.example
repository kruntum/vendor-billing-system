# Server Configuration
PORT=8801
NODE_ENV=development

# Database Configuration
# รูปแบบ: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/vendor_billing_db?schema=public"

# Authentication
# Secret Key สำหรับ JWT (ควรตั้งให้ยาวและซับซ้อนใน Production)
JWT_SECRET="your-super-secret-jwt-key-2024"
JWT_REFRESH_SECRET="your-super-secret-refresh-key-2024"

# Client URL (CORS)
# URL ของ Frontend ที่อนุญาตให้ยิง API เข้ามาได้
CLIENT_URL=http://localhost:5173

# PDF Storage Path
# Path สำหรับเก็บไฟล์ PDF ที่สร้าง (ควรเป็น Absolute Path)
# ตัวอย่าง Windows: C:\Users\YourName\Documents\vendor-billing-system\server\storage
STORAGE_PATH="./storage"
