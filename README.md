# 📦 Vendor Billing System (VBS)

![Project Banner](https://via.placeholder.com/1200x300?text=Vendor+Billing+System)

> **ระบบจัดการการวางบิลและใบเสร็จรับเงินสำหรับ Vendor**  
> พัฒนาด้วย Modern Stack เน้นความรวดเร็ว สวยงาม และใช้งานง่าย

![Status](https://img.shields.io/badge/Status-Development-yellow) ![License](https://img.shields.io/badge/License-MIT-blue)

---

## 📑 สารบัญ (Table of Contents)

1. [เกี่ยวกับโปรเจกต์ (About)](#-เกี่ยวกับโปรเจกต์-about)
2. [เทคโนโลยีที่ใช้ (Tech Stack)](#-เทคโนโลยีที่ใช้-tech-stack)
3. [ฟีเจอร์หลัก (Key Features)](#-ฟีเจอร์หลัก-key-features)
4. [โครงสร้างโปรเจกต์ (Project Structure)](#-โครงสร้างโปรเจกต์-project-structure)
5. [การติดตั้งและใช้งาน (Getting Started)](#-การติดตั้งและใช้งาน-getting-started)
   - [แบบ Docker (แนะนำ)](#-วิธีที่-1-รันด้วย-docker-แนะนำ)
   - [แบบ Manual (สำหรับนักพัฒนา)](#-วิธีที่-2-รันแบบ-manual-สำหรับนักพัฒนา)
6. [การตั้งค่า Environment Variables](#-การตั้งค่า-environment-variables)
7. [การแก้ไขปัญหา (Troubleshooting)](#-การแก้ไขปัญหา-troubleshooting)

---

## 📖 เกี่ยวกับโปรเจกต์ (About)

**Vendor Billing System (VBS)** คือเว็บแอปพลิเคชันรูปแบบ **Monorepo** ที่ออกแบบมาเพื่อช่วยจัดการกระบวนการวางบิลระหว่างบริษัท (Admin) และผู้ขาย (Vendor) ให้เป็นระบบระเบียบ

กระบวนการทำงานครอบคลุมตั้งแต่:

1.  Admin สร้างใบสั่งงาน (Jobs) ให้ Vendor
2.  Vendor รวมงานและสร้าง **ใบวางบิล (Billing Note)** ส่งให้ Admin ตรวจ
3.  Admin ตรวจสอบและอนุมัติ
4.  Vendor ออก **ใบเสร็จรับเงิน (Receipt)**
5.  Admin ออก **ใบสำคัญจ่าย (Payment Voucher)** พร้อมคำนวณภาษีหัก ณ ที่จ่าย (WHT) ให้อัตโนมัติ

---

## 🛠️ เทคโนโลยีที่ใช้ (Tech Stack)

| Component    | Technology                                                                                                                                                                                                                                                  | Description                                                        |
| :----------- | :---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :----------------------------------------------------------------- |
| **Frontend** | ![React](https://img.shields.io/badge/-React-61DAFB?logo=react&logoColor=white) ![Vite](https://img.shields.io/badge/-Vite-646CFF?logo=vite&logoColor=white) ![Tailwind](https://img.shields.io/badge/-TailwindCSS-06B6D4?logo=tailwindcss&logoColor=white) | พัฒนาด้วย React + Vite เน้นความเร็ว และตกแต่งด้วย TailwindCSS      |
| **Backend**  | ![Bun](https://img.shields.io/badge/-Bun-000000?logo=bun&logoColor=white) ![Elysia](https://img.shields.io/badge/-ElysiaJS-F06292?logo=elysia&logoColor=white)                                                                                              | ใช้ Bun Runtime และ Elysia Framework เพื่อประสิทธิภาพสูงสุด        |
| **Database** | ![PostgreSQL](https://img.shields.io/badge/-PostgreSQL-336791?logo=postgresql&logoColor=white) ![Prisma](https://img.shields.io/badge/-Prisma-2D3748?logo=prisma&logoColor=white)                                                                           | ฐานข้อมูล PostgreSQL จัดการ Schema และ Type Safety ด้วย Prisma ORM |
| **DevOps**   | ![Docker](https://img.shields.io/badge/-Docker-2496ED?logo=docker&logoColor=white)                                                                                                                                                                          | Containerization เต็มรูปแบบ แยก Service ชัดเจน                     |

---

## ✨ ฟีเจอร์หลัก (Key Features)

- **📝 จัดการใบวางบิล (Billing Management)**: Vendor สามารถเลือกงานที่ทำเสร็จแล้วมาสร้างใบวางบิลได้ง่ายๆ
- **✅ ระบบอนุมัติ (Approval Workflow)**: Admin สามารถตรวจสอบ แก้ไข หรือ Reject ใบวางบิลก่อนมีการจ่ายเงินจริง
- **🧾 ออกเอกสารอัตโนมัติ**: ระบบสร้างไฟล์ PDF สำหรับ ใบสำคัญจ่าย (PV) และ หนังสือรับรองหัก ณ ที่จ่าย (50 ทวิ) ให้อัตโนมัติ
- **🔒 ระบบจัดการสิทธิ์ (RBAC)**: แยกสิทธิ์การใช้งานชัดเจนระหว่าง Admin, User (ภายใน), และ Vendor (ภายนอก)
- **📊 Dashboard**: กราฟสรุปยอดรอจ่าย ยอดจ่ายแล้ว และสถานะเอกสารต่างๆ แบบ Real-time

---

## 📁 โครงสร้างโปรเจกต์ (Project Structure)

โปรเจกต์นี้ใช้โครงสร้างแบบ **Monorepo** เก็บทั้ง Client และ Server ไว้ในที่เดียว:

```
vendor-billing-system/
├── client/                 # 🖥️ ส่วน Frontend (React)
│   ├── src/
│   ├── public/
│   ├── Dockerfile          # Docker setup สำหรับ Client
│   └── .env.example        # ตัวอย่าง Config frontend
│
├── server/                 # ⚙️ ส่วน Backend (Elysia/Bun)
│   ├── src/
│   │   ├── routes/         # API Routes ทั้งหมด
│   │   ├── services/       # Business Logic
│   │   └── generated/      # Prisma Client ที่ถูก Generate
│   ├── prisma/            # Database Schema & Migrations
│   ├── storage/           # ที่เก็บไฟล์ PDF (Local)
│   ├── Dockerfile         # Docker setup สำหรับ Server
│   └── .env.example       # ตัวอย่าง Config server
│
├── docker-compose.yml      # 🐳 Config การจำลอง Server ทั้งระบบ
├── .dockerignore           # รายชื่อไฟล์ที่ไม่นำเข้า Docker Build
└── README.md               # คู่มือการใช้งานที่คุณกำลังอ่านอยู่นี้
```

---

## 🚀 การติดตั้งและใช้งาน (Getting Started)

### สิ่งที่ต้องมีเบื้องต้น (Prerequisites)

ก่อนเริ่มใช้งาน ต้องติดตั้งโปรแกรมเหล่านี้ในเครื่องก่อน:

1.  **[Docker Desktop](https://www.docker.com/products/docker-desktop)** (จำเป็นมาก)
2.  **[Git](https://git-scm.com/downloads)**

---

### 🔥 วิธีที่ 1: รันด้วย Docker (แนะนำ ⭐️)

วิธีนี้ง่ายที่สุด ไม่ต้องลง Node.js หรือ Bun ในเครื่องก็รันได้

1.  **Clone Project**

    ```bash
    git clone https://github.com/your-username/vendor-billing-system.git
    cd vendor-billing-system
    ```

2.  **จัดการ Environment Variables**

    - ในโฟลเดอร์ Root (`/`), `client/` และ `server/` จะมีไฟล์ `.env.example` อยู่
    - ให้ copy มาสร้างเป็นไฟล์ `.env` (หรือ `.env.local` ใน client) โดยตั้งค่าตามที่ต้องการ
    - _โดยปกติค่า Default สามารถใช้งานได้เลยสำหรับการทดสอบ Local_

3.  **Build และ Run**

    ```bash
    docker-compose up --build -d
    ```

    _รอสักครู่เพื่อให้ Docker download image และ setup database_

4.  **เข้าใช้งาน**
    - **Frontend**: เปิด Browser ไปที่ [http://localhost:8802](http://localhost:8802)
    - **Backend**: [http://localhost:8801](http://localhost:8801)
    - **Database (Prisma Studio)**: (ถ้าเปิด Port ไว้)

---

### 💻 วิธีที่ 2: รันแบบ Manual (สำหรับนักพัฒนา)

เหมาะสำหรับคนที่ต้องการแก้โค้ดและเห็นผลลัพธ์ทันที (Hot Reload)

**สิ่งที่ต้องมีเพิ่มเติม:**

- **[Bun](https://bun.sh/)** (Runtime หลักของโปรเจกต์นี้)
- **PostgreSQL** (ติดตั้งแยก หรือใช้ Docker เฉพาะ DB ก็ได้)

> [!CAUTION] > **สำคัญมากเรื่อง Prisma Version**:
> โปรเจกต์นี้ใช้ **Prisma 6.0+** (รุ่นใหม่ล่าสุด) กรุณาตรวจสอบว่า Extension ใน VSCode หรือ Global CLI ของคุณรองรับเวอร์ชั่นนี้ เพื่อป้องกัน Error เรื่อง Schema Compatibility

**ขั้นตอนการรัน:**

1.  **ติดตั้ง Dependencies** (ที่ Root Folder)

    ```bash
    bun install
    ```

2.  **Setup Database (Server)**

    ```bash
    cd server
    cp .env.example .env
    # แก้ไข DATABASE_URL ให้ตรงกับเครื่องของคุณ

    # Run Migration
    bun prisma migrate dev

    # Generate Prisma Client
    bun prisma generate

    # (Optional) Seed ข้อมูลเริ่มต้น
    bun prisma db seed
    ```

3.  **Start Services**
    - เปิด Terminal 1 (Backend):
      ```bash
      cd server
      bun dev
      ```
    - เปิด Terminal 2 (Frontend):
      ```bash
      cd client
      bun dev
      ```

---

## ⚙️ การตั้งค่า Environment Variables

### Root (`/.env`)

ใช้สำหรับ Docker Compose (ชื่อ Container, Port ภายนอก)

```ini
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
SERVER_PORT=8801
CLIENT_PORT=5173
```

### Server (`/server/.env`)

ใช้สำหรับ Backend Logic และการต่อ Database

```ini
DATABASE_URL="postgresql://..."
JWT_SECRET="secret-key"
CLIENT_URL="http://localhost:5173"
```

### Client (`/client/.env.local`)

ใช้สำหรับ Frontend เพื่อยิง API

```ini
VITE_API_URL=http://localhost:8801
```

---

## 🔧 การแก้ไขปัญหา (Troubleshooting)

### ❌ Docker: "Client cannot connect to API"

- **อาการ**: Login ไม่ได้, หน้าเว็บโหลดไม่หมด
- **สาเหตุ**: ค่า `VITE_API_URL` อาจจะผิด หรือ Port ชนกับ Service อื่น
- **วิธีแก้**:
  1. เช็คไฟล์ `.env.local` ใน client ว่า URL ปลายทางถูกต้อง (เช่น `http://localhost:8801`)
  2. เช็คว่าไม่มี Service อื่นแย่ง Port 8801 หรือ 5432
  3. ตรวจสอบ `.dockerignore` ว่าไม่ได้เผลอเอาไฟล์ `.env` แปลกปลอมติดเข้าไปตอน Build

### ❌ Server: "Cannot find module .../generated/prisma/client"

- **อาการ**: Server รันไม่ขึ้นใน Docker แจ้งว่าหาไฟล์ Prisma ไม่เจอ
- **สาเหตุ**: การ Copy ไฟล์ใน Dockerfile ขั้นตอน Runner ไปทับไฟล์ที่ Generate ไว้
- **วิธีแก้**: ตรวจสอบ `server/Dockerfile` ว่ามีบรรทัดนี้หรือไม่:
  ```dockerfile
  COPY --from=builder /app/src/generated ./src/generated
  ```

---

Develop with ❤️ by Antigravity
